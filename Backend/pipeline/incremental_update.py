# this is just a rough outline for now

# def update_master_with_new_zip(new_zip_path):
#     df_new = process_single_zip(new_zip_path)

#     if master_exists:
#         master_df = load_master()
#         master_df = pd.concat([master_df, df_new])
#     else:
#         master_df = df_new

#     save_master(master_df)